{
  "name": "project/app",
  "scripts": {
    "test": "vendor/bin/phpunit tests --colors=always --testdox"
  },
  "require-dev": {
    "phpunit/phpunit": "^11.5"
  },
  "require": {
    "vlucas/phpdotenv": "^5.6"
  }
}
